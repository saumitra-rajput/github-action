name: greet
on:
  push:
    branches:
      - dev
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: greet
        run: echo "hello Jarvis!"
      - name: say hello to Runner
        run: echo "Hey runner ${USER}"
